/*
extern crate rug;
extern crate johnston;

use rug::Rational;
use johnston::{otonal_step, walk};

#[test]
fn it_walks_otonally() {
    // Arrange.
    let fifth = Rational::from((3, 2));

    // Act.
    let z = walk(&otonal_step, 5, &fifth);

    // Assert.
    assert_eq!(z, vec![(1, 1), (3, 2), (9, 8), (27, 16), (81, 64)]);
}
*/
